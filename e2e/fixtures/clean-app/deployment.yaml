apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  labels:
    owner: team-a
spec:
  template:
    spec:
      containers:
        - name: app
          image: registry.corp.internal/web:1
          resources:
            limits:
              memory: 128Mi
